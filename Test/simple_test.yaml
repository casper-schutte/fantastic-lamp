name: simple-test

on:
  push:
    branches: ["master"]
  pull_request:
    branches: ["master"]
jobs:
  install requirements:
    steps:

  run script:
    steps:
      - name: test script
        run: find_coverage_test.sh
        shell: bash
